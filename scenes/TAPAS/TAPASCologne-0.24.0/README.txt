the network is built in two steps
1) with cologne.netccfg to import the OSM data
2) with cologne2.netccfg to remove unconnected components

the demand is reduced via --scale to avoid jamming (caused mostly by invalid intersections and traffic lights)
